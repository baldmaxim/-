# Python
__pycache__/
*.py[cod]

# Excel — для людей, не для git
data/*.xlsx
data/_tmp_*.xlsx

# JSON — для данных, ВКЛЮЧАЕМ в git (версионирование)
# data/*.json  ← НЕ игнорируем, они должны быть в git

# MD — для ИИ, ВКЛЮЧАЕМ в git (база знаний)
# *.md  ← НЕ игнорируем

# Логи
*.log

# Виртуальное окружение
venv/
.env/

# IDE
.vscode/
.idea/
